# Codex fixture status

No Codex protocol fixture exists yet. The Phase 0 execution identity could discover the CLI through the installed desktop application package but could not execute even `codex --version`; Windows returned access denied before any CLI output.

This must be re-probed from the owner's interactive terminal before Phase 3. Capture only sanitized version/help/new-session/resume/error/cancel fixtures. Do not alter package permissions, inspect credentials, or substitute browser automation.
