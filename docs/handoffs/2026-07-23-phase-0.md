# Phase handoff

- Date/time: 2026-07-23
- Phase: 0 — Provider feasibility spike
- Agent/model: Codex
- Working tree/branch: initial uncommitted blueprint tree
- Status: Complete

## Outcome first

Claude Code 2.1.217 is feasible for a safe, read-only Personal Mode adapter with streaming JSON, opaque session resume, stdin prompt transport, and forced cleanup of owned child processes. Codex is not validated because the current automated execution identity cannot execute the desktop-package CLI; this is an environment access block, not a product conclusion.

## Scope completed

- Discovered safe local environment/runtime/provider availability.
- Performed bounded Claude probes in an empty temporary workspace.
- Verified stream/resume/stdin/cwd/read-only candidate/controlled invalid session/owned-child cleanup.
- Captured only sanitized artifacts and generated a capability matrix/report.
- Did not install/update providers, alter login state, read credentials, add product scaffolding, or touch a user project workspace.

## Files changed

- `docs/STATUS.md`: Phase 0 in progress; updated to complete in final transition.
- `docs/generated/PROVIDER-SPIKE-REPORT.md`: conclusion and evidence.
- `docs/generated/provider-capabilities.json`: machine-readable matrix.
- `fixtures/providers/**`: sanitized observed and synthetic parser fixtures.
- `spikes/provider-probe/README.md`: safe re-probe instructions.
- `docs/handoffs/2026-07-23-phase-0.md`: this evidence record.

## Decisions and deviations

- ADRs added/referenced: ADR-0002, ADR-0003.
- No Codex JSONL test was possible because the executable returned Windows access denied even under elevated read-only execution. The plan permits one-provider progress, so this is a recorded limitation rather than an unsafe workaround.
- Graceful provider-level Claude cancellation was not proved. Forced cleanup is recorded separately and capability remains false.

## Verification evidence

| Command/test | Result | Notes |
|---|---|---|
| `claude --version` | Pass | 2.1.217 |
| Claude help capability inspection | Pass | `stream-json`, `resume`, `plan`, stdin flags observed |
| New stream JSON run | Pass | Requires `--verbose`; init/deltas/result observed |
| Resume known session | Pass | Fixed response returned through same opaque session |
| Stdin prompt run | Pass | Fixed response returned |
| Plan-mode write request | Pass with limitation | No file created; run reached max-turns during plan |
| Invalid session | Pass | Controlled error result |
| Owned child cleanup | Pass with limitation | Force termination; not graceful provider cancel |
| `codex --version` | Blocked | Windows access denied before output |
| Fixture/report secret/path scan | Pass | No raw session IDs, user paths, or credential-like strings found |

## Acceptance checklist

- [x] Exact installed versions and platform constraints recorded.
- [x] Streaming text can be incrementally parsed for every GO provider.
- [x] Opaque provider session can be captured/resumed for Claude.
- [x] Read-only ceiling verified without bypass flags for Claude MVP.
- [x] Authentication remained provider-owned.
- [x] Sanitized happy path/failure corpus added.
- [x] Go/no-go decision and adapter recommendation explicit.
- [x] No product framework/dependency scaffold added.

## Known issues and risks

- Codex must be re-probed from an owner-interactive terminal before Phase 3.
- Claude raw `system/init` output is rich and may contain sensitive local metadata; Phase 4 must field-allowlist, not redact after broad capture.
- Claude `stream-json` requires `--verbose` in observed version.
- Tools, approvals, image input, workspace writes, and graceful cancellation remain disabled/unverified.

## Exact next action

Run the Phase 0 artifact validation (JSON, fixture hygiene, no accidental raw IDs/paths), update `STATUS.md` to mark Phase 0 Complete and Phase 1 Ready, then start `docs/phases/PHASE-1-foundation.md` only after those checks pass.
