# Implementation handoffs

Future agents add one timestamped Markdown file per phase attempt using `docs/templates/HANDOFF.md`.

Handoffs are evidence, not marketing summaries. Include exact checks, failures, unverified criteria, provider versions, decisions, and the next concrete action. Never include credentials, raw sensitive provider output, or personal absolute paths.
