# Phase 2 work in progress (superseded)

Phase 2 is complete. The authoritative final handoff is
`docs/handoffs/2026-07-24-phase-2.md`.

- Date/time: 2026-07-24, Asia/Taipei
- Phase: PHASE-2-local-chat
- Status: In progress

## Implemented slice

- Added provider-neutral fake-run contracts to packages/contracts.
- Added a Markdown-aware mention parser and deterministic resolution/group expansion to packages/core.
- Added a deterministic, in-process fake provider that streams a scripted event list and honours cancellation before the next event.
- Added migration 0002 for the initial local-chat tables, durable room events,
  idempotency records, and FTS5 message index.
- Added typed room repository access through the database handle. Room creation
  and human message writes create durable events in one SQLite transaction.

## Evidence

| Command | Result |
|---|---|
| pnpm --filter @models-roundtable/core test | Pass: parser and core boundary tests |
| pnpm --filter @models-roundtable/provider-fake test | Pass: scripted streaming and cancellation test |
| pnpm --filter @models-roundtable/db test:integration | Pass: temporary SQLite database applies migrations 0001 and 0002 |
| Room repository integration test | Pass: create room, ordered message event, idempotent retry, and mismatched retry rejection |

## Not yet implemented

- Agent participants, mentions, context snapshots, queued runs, and the atomic
  command service that combines them.
- Atomic message command/idempotency, run supervisor, replay, cancellation persistence, search, export, WebSocket protocol, and room UI.

## Exact next action

Implement Phase 2 SQLite repositories and the atomic message.send command before exposing any new server endpoint. Keep agent-authored messages unable to create new runs.
