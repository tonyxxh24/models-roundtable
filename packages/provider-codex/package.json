{
  "name": "@models-roundtable/provider-codex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "smoke:owner": "pnpm run build && node dist/owner-smoke.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@models-roundtable/contracts": "workspace:*",
    "@models-roundtable/provider-base": "workspace:*"
  },
  "devDependencies": {
    "@models-roundtable/test-support": "workspace:*"
  }
}
